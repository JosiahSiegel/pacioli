# Task 8 — Integrated quality gates

Date: 2026-08-13
Worktree: `S:/repos/pacioli-worktrees/environment-exclusion`
Branch: `feat/environment-exclusion`
Runtime: CPython 3.14.3, pytest 9.1.1, pytest-playwright 0.7.2, Playwright 1.57.0, Chromium.

## Required gates

| Command | Fresh result |
| --- | --- |
| `make sync-mappings` | PASS — mapping mirror synchronized; exit 0. |
| `python -m pytest scanner/tests/test_discovery.py scanner/tests/test_orchestrator.py scanner/tests/test_aggregate_html.py -v` | PASS — 157 passed, 2 warnings, 160.27s; exit 0. |
| `make test` | PASS — 608 passed, 6 skipped, 4 browser tests deselected, 10 upstream deprecation warnings, 364.70s; exit 0. Browser remains optional for ordinary tests. |
| `make test-browser` | PASS — 4 passed in 7.84s; exit 0. Chromium browser coverage remains in the dedicated target. |
| `make lint` | PASS — `ruff check scanner/`: all checks passed; exit 0. |
| `make selftest` | PASS — `safety_selftest: PASS`; pinned Checkov `3.3.9`; exit 0. |

## Browser visual and runtime evidence

Generated by `test_report_visual_evidence_at_responsive_theme_and_motion_contracts` in `scanner/tests/test_report_browser.py`; screenshots are fresh and stored under:

`S:/repos/pacioli-worktrees/environment-exclusion/.omo/evidence/environment-exclusion/task-8-screenshots/`

| Viewport | Dark | Light | System (OS-light) | Reduced motion |
| --- | --- | --- | --- | --- |
| 375 px | `375-dark.png` | `375-light.png` | `375-system.png` | `375-reduced-motion.png` |
| 768 px | `768-dark.png` | `768-light.png` | `768-system.png` | `768-reduced-motion.png` |
| 1280 px | `1280-dark.png` | `1280-light.png` | `1280-system.png` | `1280-reduced-motion.png` |

Additional state captures:

- `labeled-stack.png` — Environments route (`#environments`) with separate `payments / prod [blue]` and `payments / prod [green]` table cells.
- `all-environments-excluded.png` — all three checkboxes selected; explicit health empty state plus `NO VISIBLE ENVIRONMENTS` coverage state.
- `local-storage-denied-dark.png` — injected `SecurityError` for both `Storage.prototype.getItem` and `setItem`; report remains dark-first and renders without page or console errors.

Assertions from the browser harness:

- At every responsive width, dark first render resolves `--color-bg` to `#121820`; persisted light resolves it to `#f5f7fb`; system state is persisted and rendered under emulated OS light.
- Reduced-motion media query is active and the theme control transition duration is at most `0.00001` seconds; no animation was disabled outside that user-preference mode.
- Theme persistence is demonstrated through localStorage writes plus reloads. Environment exclusion persistence is demonstrated by reloading with three checked exclusion controls, then restoring the full report.
- Storage denial is exercised at runtime, not only asserted in generated source: guarded localStorage access falls back to dark with no errors.
- `pageerror` fails the visual test immediately; all `console.error` messages are collected and must be empty. The final browser target passed all four tests.

## Cross-surface verification

- Public documentation/schema regression is included in `make test` through `test_environment_exclusion_docs.py`; all non-browser tests passed.
- Browser dependencies remain pinned in `pyproject.toml`: `pytest-playwright==0.7.2` and `playwright==1.57.0`.
- The CI browser-install workflow remains unchanged: dedicated `browser` job installs `.[test]`, runs `python -m playwright install --with-deps chromium`, then `make test-browser`.
- `Makefile` still deselects `browser` tests for `make test` and runs them only through `make test-browser`.
- Report runtime boundary was inspected: it remains standalone HTML/CSS/vanilla JavaScript; no framework, telemetry, or network calls were added. The harness serves generated content only on ephemeral `127.0.0.1`.

## Diagnostics and cleanup

- `ruff check scanner/tests/test_report_browser.py`: PASS.
- The changed test module is 203 pure LOC, under the 250-LOC review ceiling.
- LSP diagnostics could not be run because the active Ruff LSP root is the separate `S:/repos/pacioli` checkout rather than this task-owned worktree; the repository's required Ruff lint gate passed cleanly.
- No test failures, browser traces, or generated product artifacts remain outside the committed Task 8 evidence directory.
